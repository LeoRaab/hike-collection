import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hike-Collection',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
