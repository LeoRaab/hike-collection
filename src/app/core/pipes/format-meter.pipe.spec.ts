import { FormatMeterPipe } from './format-meter.pipe';

describe('FormatMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
